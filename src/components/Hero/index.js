import React from 'react';
import { HeroContainer } from './HeroElements';

const Hero = () => {
    return (
        <HeroContainer>
            
        </HeroContainer>
    )
}

export default Hero;
